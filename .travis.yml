language: csharp
dotnet: 2.2
dist: xenial
mono: latest
solution: HMS.sln
before_depoly:
 - cd ~
script:
 - "travis_wait 30 sleep 1800 &"
 - rm docker-compose.*
 - dotnet restore
 - dotnet build -c Release Data/
 - dotnet build -c Release Data.Simulate.AQUATOX/
 - dotnet build -c Release HMS.sln
 - dotnet build -c Release Web.Services/
 - dotnet build -c Release Web.Services.Tests/
 - sudo mkdir /app
 - sudo mkdir ~/Coverage
 - sudo cp -r Web.Services/App_Data /app/App_Data
 - sudo cp Web.Services/App_Data/* /home/travis/build/quanted/hms/Web.Services.Tests/bin/Release/netcoreapp2.2/
 - dotnet test Data.Tests/Data.Tests.csproj -c Release --collect:"Code Coverage" /p:CollectCoverage=true
 - dotnet test Web.Services.Tests/Web.Services.Tests.csproj -c Release --collect:"Code Coverage" /p:MergeWith="Data.Tests/coverage.json" /p:CollectCoverage=true
 - dotnet test Precipitation.Tests/Precipitation.Tests.csproj -c Release --collect:"Code Coverage" /p:MergeWith="Web.Services.Tests/coverage.json" /p:CollectCoverage=true
 - dotnet test OrganicMatter/OrganicMatter.AQUATOX.UnitTests/OrganicMatter.AQUATOX.UnitTests.csproj -c Release --collect:"Code Coverage" /p:MergeWith="Precipitation.Tests/coverage.json" /p:CollectCoverage=true
 - dotnet test Stream.Hydrology/AQUATOX.UnitTests/Stream.Hydrology.AQUATOX.UnitTests.csproj -c Release --collect:"Code Coverage" /p:MergeWith="OrganicMatter/OrganicMatter.AQUATOX.UnitTests/coverage.json" /p:CollectCoverage=true
 - dotnet test Nutrients/Nutrients.AQUATOX.UnitTests/Nutrients.AQUATOX.UnitTests.csproj -c Release --collect:"Code Coverage" /p:MergeWith="Stream.Hydrology/AQUATOX.UnitTests/coverage.json" /p:CollectCoverage=true /p:CoverletOutputFormat=opencover 
 - sudo cp Nutrients/Nutrients.AQUATOX.UnitTests/coverage.opencover.xml ~/Coverage/coverage.opencover.xml
 - sudo ls ~/Coverage 
 - dotnet tool install coveralls.net --version 1.0.0 --tool-path ~/tools
 - ~/tools/csmacnz.Coveralls --opencover -i ~/Coverage/coverage.opencover.xml --repoToken $COVERALLS_REPO_TOKEN --commitId $TRAVIS_COMMIT --commitBranch $TRAVIS_BRANCH --commitMessage "$REPO_COMMIT_MESSAGE" --jobId $TRAVIS_JOB_ID --serviceName "travis-ci"