using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace Solar.Tests
{
    [TestClass]
    public class SolarUTests
    {
        [TestMethod]
        public void TestValidWavelengths()
        {
            //GCSolar solar = new GCSolar();
            //solar.common.maxwav = 10;
            //solar.common.minwav = 8;
            //double[] wave = { 297.5, 300.0, 302.5 };
            //double[] water = { 1.12, 0.9, 0.75 };
            //double[] chem = { 0.4, 0.3, 0.22 };
            //DataTable dt = new DataTable();
            //DataRow dr1 = null;
            //dt.Columns.Add("Wavelength (nm)");
            //dt.Columns.Add("Water Attenuation Coefficients (m**-1)");
            //dt.Columns.Add(solar.common.contaminantType.Trim() + " Absorption Coefficients (L/(mole cm))");

            //for (int i = 0; i < 3; i++)
            //{
            //    dr1 = dt.NewRow();
            //    dr1["Wavelength (nm)"] = wave[i];
            //    dr1["Water Attenuation Coefficients (m**-1)"] = water[i];
            //    dr1[solar.common.contaminantType.Trim() + " Absorption Coefficients (L/(mole cm))"] = chem[i];
            //    dt.Rows.Add(dr1);
            //}

            //FormAbsorptions form1 = new FormAbsorptions();
            //string data = "All";
            //string errorMessage = "";
            //form1.validateInputFile(dt, data, out errorMessage);

            //Assert.AreEqual("", errorMessage);
        }
    }
}
